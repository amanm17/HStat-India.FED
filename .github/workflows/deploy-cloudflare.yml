name: Deploy HStat India

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled directly by Cloudflare Pages from the GitHub repository."
